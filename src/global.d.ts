// Injected by vite.config.ts's `define`, not real globals — see the comment
// there for why this exists.
declare const __BUILD_SHA__: string
declare const __BUILD_TIME__: string
declare const __BUILD_RUN__: string
